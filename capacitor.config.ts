import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.dlaroslin.app',
  appName: 'dlaroslin.pl',
  webDir: 'build',
  ios: {
    path: 'ios'
  }
};

export default config;

